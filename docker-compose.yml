services:
  redline:
    image: ghcr.io/tyler-irving/redlinefish:latest
    container_name: redline
    env_file:
      - .env
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads